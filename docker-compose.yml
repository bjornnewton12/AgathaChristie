services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5018:8080"
    volumes:
      - ./data:/app/data
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__Default=Data Source=/app/data/agatha.db
